name: flutter_fbdev_example
description: Minimal flutter_fbdev handheld example - shows pressed buttons.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.5.0
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_fbdev:
    path: ../

dev_dependencies:
  flutter_lints: ^5.0.0
